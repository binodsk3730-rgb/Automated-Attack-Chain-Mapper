# AACM - Automated Attack Chain Mapper

A graph-based cybersecurity analysis tool that maps attack paths through networks using **DFS**, **BFS**, and **Dijkstra** algorithms. Built from scratch in C++17/20 with STL-only data structures (plus a single JSON parsing header).

## Architecture